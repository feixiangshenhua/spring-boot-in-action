application:
  version: 1.1.0-SNAPSHOT
server:
  port: 8093
  servlet:
    context-path: /nari-oauth2-server

# 配置服务发现
eureka:
  client:
    instanceInfoReplicationIntervalSeconds: 10
    enabled: false
    serviceUrl:
      defaultZone: http://10.100.1.19:8761/eureka/
  instance:
    preferIpAddress: false

spring:
  datasource:
    # metabase的数据源配置
    metabase:
      type: com.zaxxer.hikari.HikariDataSource
#      jdbc-url: jdbc:oracle:thin:@10.100.1.20:1521:dicp
#      username: metabase
#      password: metabase
      jdbc-url: jdbc:oracle:thin:@172.16.221.214:1521:GDOMSDB
      username: DICP
      password: DICP
      driverClassName: oracle.jdbc.driver.OracleDriver
      hikari:
        maximum-pool-size: 10
        minimum-idle: 10
    # pi6000的数据源，用于读取用户信息
    pi6000:
      type: com.zaxxer.hikari.HikariDataSource
      jdbc-url: jdbc:oracle:thin:@172.16.221.214:1521:GDOMSDB
      username: OMS_PI6000
      password: OMS_PI6000
      driverClassName: oracle.jdbc.driver.OracleDriver
      hikari:
        maximum-pool-size: 8
        minimum-idle: 2
  redis:
    host: 192.168.3.168
    port: 6379
    database: 2
#    password:
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        max-wait: 1ms
        min-idle: 0
      shutdown-timeout: 100ms
  security:
    user:
      name: admin
      password: oms_admin

# logback配置文件路径
logging:
  config: classpath:logback.xml

